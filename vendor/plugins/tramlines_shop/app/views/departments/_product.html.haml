%li
  =link_to product, product
  -bracket(:class => 'admin_links') do
    -separate_links do
      =link_if_allowed 'Edit', edit_product_proc(product)
      =link_if_allowed 'Delete', product, :method => :delete, :confirm => "Are you sure you want to delete this product?"
